{
  "name": "@app/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "start": "bun --env-file=../../.env run src/index.ts",
    "verify:queue": "bun run --env-file=../../.env scripts/enqueue-smoke.ts",
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@db/processor": "workspace:*",
    "@shared/processor": "workspace:*",
    "bullmq": "^5.78.0",
    "hono": "^4.6.0",
    "ioredis": "5.10.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^6.0.3"
  }
}
